# Take length and width and display area of rect

# Input
length = float(input("Enter length :"))
width = float(input("Enter width :"))
# Process
area = length * width
# Output
print(f"Area of rectangle with length = {length:.2f}, width = {width:.2f} is {area:.2f}")

